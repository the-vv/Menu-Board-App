import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thevv.menuboard',
  appName: 'MenuBoard',
  webDir: 'dist/frontend/browser'
};

export default config;
